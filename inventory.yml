all:
  vars:
    ansible_python_interpreter: /usr/bin/python3.12

  hosts:
    head:
      ansible_host: localhost
      ansible_connection: local
      vm_role: head

    vm-01:
      ansible_host: 10.12.0.111
      vm_role: client
